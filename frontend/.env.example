# Backend API Base URL
# For local development with Vite proxy, leave empty or use http://localhost:5001/api
# For production deployment (e.g. Render/Railway backend), set the live backend URL
VITE_API_URL=http://localhost:5001/api
